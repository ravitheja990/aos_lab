# aos_lab

Setup Instructions:

Clone/Fork aos_lab repo

install gcc c compiler:

Use this command to create executable test file:
gcc -o test test.c
Run the executable test file using this command:
./test [MEM] [N_PROC]
